rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Onboarding files are private and only handled by the REST API.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
